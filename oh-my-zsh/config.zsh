# http://blog.ysmood.org/my-ys-terminal-theme/
ZSH_CUSTOM="$HOME/.dotfiles/oh-my-zsh/custom"
ZSH_THEME="chussenot"

plugins=(zsh-reload rust k git sudo tmux tmuxinator ruby gem aws bower bundler brew-cask brew coffee docker git-extras lein node npm osx pip python terraform scala sbt z docker-compose gradle mvn ng pass perl tig helm minikube nmap nomad nvm perl ssh-agent vault yarn)

source $ZSH/oh-my-zsh.sh
